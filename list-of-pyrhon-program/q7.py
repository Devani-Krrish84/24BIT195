def minutes_to_hours(minutes):
    return minutes / 60

# Example usage
minutes = float(input("Enter the number of minutes: "))
print(f"{minutes} minutes is equal to {minutes_to_hours(minutes)} hours.")